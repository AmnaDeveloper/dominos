User-agent: *
Allow: /
Disallow: /api/
Disallow: /admin/
Disallow: /private/
Disallow: /seo-tools

# PWA manifest — a JSON file, not a page. Google was crawling it and filing it
# under "Crawled - currently not indexed", which is correct but noisy.
# Blocking it here affects crawlers only; browsers still install the PWA.
Disallow: /manifest.webmanifest

User-agent: Mediapartners-Google
Allow: /
Allow: /ads.txt

User-agent: AdsBot-Google
Allow: /
Allow: /ads.txt

User-agent: Googlebot-Image
Allow: /

Sitemap: https://dominos-menu.us/sitemap.xml
